# Frontend notes

Loose scratch that hasn't earned a real home yet.

- Framer Motion 12 changed the `layout` prop semantics — check list
  reordering on the orders table.
- Server component + client component boundaries: favor passing plain JSON,
  not class instances. Hydration mismatch bit us twice.
